<div class="p-6 bg-gray-100 min-h-screen flex flex-col gap-6">
  <!-- Header -->
  <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
    <h2 class="text-3xl font-bold text-gray-800 flex items-center gap-2">
      <svg xmlns="http://www.w3.org/2000/svg" class="w-7 h-7 text-green-600" fill="none" viewBox="0 0 24 24"
        stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
      </svg>
      👤 Thêm tài khoản
    </h2>

    <a href="/dashboard/accounts"
      class="inline-flex items-center gap-2 bg-gradient-to-r from-blue-600 to-indigo-600 text-white px-5 py-2.5 rounded-xl shadow-md hover:shadow-lg hover:from-blue-700 hover:to-indigo-700 transition duration-200 font-medium">
      Quay lại
    </a>
  </div>

  <!-- Nội dung form -->
  <div class="bg-white shadow-md rounded-2xl border border-gray-200 p-8 space-y-6">
    <%- include('../error') %>

      <form method="POST" class="space-y-6">
        <div class="grid sm:grid-cols-2 gap-6">

          <!-- Tên đăng nhập -->
          <div>
            <label for="username" class="text-sm text-gray-500 mb-1 block">Tài khoản</label>
            <input type="text" id="username" name="username" placeholder="Nhập tên tài khoản" required
              value="<%= account ? account.username : '' %>"
              class="w-full border border-gray-200 rounded-lg px-4 py-2 text-gray-800 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 shadow-sm" />
          </div>

          <div class="grid sm:grid-cols-2 gap-6">
            <!-- Mật khẩu -->
            <div>
              <label for="password" class="text-sm text-gray-500 mb-1 block">Mật khẩu</label>
              <input type="password" id="password" name="password" placeholder="Nhập mật khẩu" required
                class="w-full border border-gray-200 rounded-lg px-4 py-2 text-gray-800 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 shadow-sm" />
            </div>

            <!-- Vai trò -->
            <div>
              <label for="role" class="text-sm text-gray-500 mb-1 block">Vai trò</label>
              <select name="role" id="role" required
                class="w-full border border-gray-200 rounded-lg px-4 py-2 text-gray-800 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 shadow-sm">
                <option value="Reader" <%=account && account.role==='Reader' ? 'selected' : '' %>>Độc giả</option>
                <option value="Librarian" <%=user.role !=='Admin' ? 'disabled' : '' %>
                  <%= account && account.role==='Librarian' ? 'selected' : '' %>>Thủ thư
                </option>
                <option value="Admin" <%=user.role !=='Admin' ? 'disabled' : '' %>
                  <%= account && account.role==='Admin' ? 'selected' : '' %>>Quản trị viên
                </option>
              </select>
              <% if (user.role !=='Admin' ) { %>
                <p class="text-xs text-gray-500 italic mt-1">
                  (Chỉ Admin mới có thể tạo tài khoản Thủ thư hoặc Quản trị viên)
                </p>
                <% } %>
            </div>
          </div>
          <!-- Họ và tên -->
          <div>
            <label for="fullname" class="text-sm text-gray-500 mb-1 block">Họ và tên</label>
            <input type="text" id="fullname" name="fullname" placeholder="Nhập họ và tên" required
              value="<%= account ? account.fullname : '' %>"
              class="w-full border border-gray-200 rounded-lg px-4 py-2 text-gray-800 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 shadow-sm" />
          </div>

          <!-- Email -->
          <div>
            <label for="email" class="text-sm text-gray-500 mb-1 block">Email</label>
            <input type="email" id="email" name="email" placeholder="Nhập email" required
              value="<%= account ? account.email : '' %>"
              class="w-full border border-gray-200 rounded-lg px-4 py-2 text-gray-800 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 shadow-sm" />
          </div>

          <!-- Địa chỉ -->
          <div>
            <label for="address" class="text-sm text-gray-500 mb-1 block">Địa chỉ</label>
            <input type="text" id="address" name="address" placeholder="Nhập địa chỉ" required
              value="<%= account ? account.address : '' %>"
              class="w-full border border-gray-200 rounded-lg px-4 py-2 text-gray-800 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 shadow-sm" />
          </div>

          <!-- Số điện thoại -->
          <div>
            <label for="phone" class="text-sm text-gray-500 mb-1 block">Số điện thoại</label>
            <input type="text" id="phone" name="phone" placeholder="Nhập số điện thoại" required
              value="<%= account ? account.phone : '' %>"
              class="w-full border border-gray-200 rounded-lg px-4 py-2 text-gray-800 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 shadow-sm" />
          </div>
        </div>

        <!-- Nút hành động -->
        <div class="flex justify-end gap-3 pt-6 border-t border-gray-100">
          <button type="submit"
            class="cursor-pointer bg-green-600 text-white px-5 py-2.5 rounded-xl hover:bg-green-700 transition font-medium shadow-md hover:shadow-lg">
            💾 Lưu tài khoản
          </button>
        </div>
      </form>
  </div>
</div>