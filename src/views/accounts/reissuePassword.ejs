<div class="max-w-md mx-auto bg-white shadow-xl rounded-2xl p-8 mt-12 border border-gray-100">
  <h1 class="text-2xl font-bold text-gray-800 mb-6 flex items-center gap-2">
    <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 text-green-600" fill="none" viewBox="0 0 24 24"
      stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
        d="M12 11c0-1.1.9-2 2-2h2a2 2 0 012 2v1m-6 8v-8m0 8h-3m3 0h3m0 0V9a4 4 0 00-8 0v2" />
    </svg>
    Cấp lại mật khẩu
  </h1>

  <%- include('../error') %>

    <form method="POST" class="space-y-6">
      <div class="bg-gray-50 border border-gray-200 rounded-xl p-4">
        <h2 class="text-gray-700 text-base">
          Cấp lại mật khẩu mới cho
          <strong class="text-gray-900">
            <%= account ? account.user.fullname : '' %>
          </strong>
        </h2>
      </div>

      <div>
        <label for="password" class="block text-sm font-medium text-gray-700 mb-2">Mật khẩu mới</label>
        <input type="password" id="password" name="password" placeholder="Nhập mật khẩu mới" required
          class="w-full border border-gray-300 rounded-xl px-4 py-2.5 text-gray-700 focus:ring-2 focus:ring-green-500 focus:border-green-500 transition duration-200" />
      </div>

      <div class="flex items-center justify-between gap-3 pt-4">
        <a href="/accounts"
          class="px-5 py-2.5 rounded-xl border border-gray-300 text-gray-600 hover:bg-gray-100 transition font-medium">
          Quay lại
        </a>
        <button type="submit"
          class="px-5 py-2.5 rounded-xl bg-gradient-to-r from-green-500 to-emerald-600 text-white font-medium shadow-md hover:shadow-lg hover:from-green-600 hover:to-emerald-700 transition duration-200">
          💾 Lưu mật khẩu
        </button>
      </div>

    </form>
</div>