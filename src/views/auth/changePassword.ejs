<div class="flex min-h-screen items-center justify-center bg-gradient-to-br from-slate-50 to-slate-100 px-4">
  <div class="bg-white border border-slate-200 shadow-sm rounded-2xl p-8 w-full max-w-md transition-all duration-300 hover:shadow-lg">
    <h1 class="text-2xl font-semibold text-center text-slate-800 mb-6 flex items-center justify-center gap-2">
      <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 11c1.104 0 2-.896 2-2V7a2 2 0 10-4 0v2c0 1.104.896 2 2 2z" />
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 11V9a6 6 0 1112 0v2m-9 4h6m2 0a2 2 0 012 2v3a2 2 0 01-2 2H7a2 2 0 01-2-2v-3a2 2 0 012-2h10z" />
      </svg>
      Đổi mật khẩu
    </h1>

    <%- include('../error') %>

    <form action="/account/change-password" method="POST" class="space-y-5">
      <!-- Mật khẩu hiện tại -->
      <div class="space-y-1.5">
        <label for="currentPassword" class="text-sm font-medium text-slate-700">Mật khẩu hiện tại</label>
        <input
          type="password"
          id="currentPassword"
          name="currentPassword"
          required
          placeholder="••••••••"
          class="w-full rounded-lg border border-slate-300 bg-slate-50 px-3 py-2.5 text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition"
        />
      </div>

      <!-- Mật khẩu mới -->
      <div class="space-y-1.5">
        <label for="newPassword" class="text-sm font-medium text-slate-700">Mật khẩu mới</label>
        <input
          type="password"
          id="newPassword"
          name="newPassword"
          required
          placeholder="Ít nhất 6 ký tự"
          minlength="6"
          class="w-full rounded-lg border border-slate-300 bg-slate-50 px-3 py-2.5 text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition"
        />
      </div>

      <!-- Xác nhận mật khẩu -->
      <div class="space-y-1.5">
        <label for="confirmPassword" class="text-sm font-medium text-slate-700">Xác nhận mật khẩu mới</label>
        <input
          type="password"
          id="confirmPassword"
          name="confirmPassword"
          required
          placeholder="Nhập lại mật khẩu mới"
          minlength="6"
          class="w-full rounded-lg border border-slate-300 bg-slate-50 px-3 py-2.5 text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition"
        />
      </div>

      <!-- Nút submit -->
      <button
        type="submit"
        class="w-full bg-blue-600 text-white py-2.5 rounded-lg font-medium hover:bg-blue-700 active:scale-[0.98] transition duration-200"
      >
        💾 Lưu thay đổi
      </button>

      <!-- Quay lại -->
      <div class="text-center mt-4">
        <a href="/dashboard/account" class="text-sm text-slate-600 hover:text-blue-600 hover:underline transition">
          ⬅️ Quay lại tài khoản
        </a>
      </div>
    </form>
  </div>
</div>
