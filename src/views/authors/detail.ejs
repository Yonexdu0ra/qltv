<div class="mx-6 my-6 px-6 py-8 bg-white shadow-xl rounded-2xl mt-6 border border-gray-100">

  <!-- Header -->
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-8 border-b pb-4">
    <h1 class="text-3xl font-bold text-gray-800 flex items-center gap-2">
      <span class="text-blue-600">📚</span> Chi tiết tác giả
    </h1>
    <a href="/dashboard/authors"
      class="mt-4 sm:mt-0 inline-flex items-center gap-2 bg-gradient-to-r from-blue-500 to-indigo-600 text-white px-5 py-2.5 rounded-xl text-sm font-medium shadow-md hover:shadow-lg transition">
       Quay lại danh sách
    </a>
  </div>

  <!-- Thông tin tác giả -->
  <div class="bg-white border border-blue-100 p-6 rounded-xl shadow-sm mb-10">
    <p class="text-lg font-semibold text-gray-800 mb-2">
      <span class="text-blue-700 font-bold">Tên:</span> Khaled Hosseini
    </p>
    <p class="text-gray-700 mt-2 leading-relaxed">Tác giả Afghanistan với những tiểu thuyết đầy cảm động như 'Người đua
      diều'.</p>
  </div>

  <!-- Danh sách sách -->
  <h2 class="text-2xl font-semibold text-gray-800 mb-6 flex items-center gap-2">
    📖 Danh sách sách của tác giả
  </h2>

  <% if (author.books && author.books.length> 0) { %>
    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
      <% author.books.forEach(book=> { %>
        <div
          class="group bg-white border border-gray-100 rounded-2xl shadow-sm hover:shadow-lg hover:-translate-y-1 transition transform duration-200 p-5 flex flex-col">
          <div class="flex-1">
            <h3 class="text-lg font-bold text-gray-800 mb-2 group-hover:text-blue-600 transition">
              <%= book.title %>
            </h3>
            <% if (book.published_year) { %>
              <p class="text-sm text-gray-600 mb-1">📅 <strong>Năm:</strong>
                <%= book.published_year %>
              </p>
              <% } %>
                <% if (book.isbn) { %>
                  <p class="text-sm text-gray-600 mb-1">🔖 <strong>ISBN:</strong>
                    <%= book.isbn %>
                  </p>
                  <% } %>
                    <% if (book.description) { %>
                      <p class="text-sm text-gray-600 mt-2 line-clamp-3">
                        <%= book.description %>
                      </p>
                      <% } %>
          </div>

          <div class="mt-5">
            <a href="/books/<%= book.id %>"
              class="inline-block w-full text-center bg-gradient-to-r from-blue-500 to-indigo-600 text-white px-4 py-2.5 rounded-xl text-sm font-medium shadow hover:from-blue-600 hover:to-indigo-700 hover:shadow-md transition">
              Xem chi tiết
            </a>
          </div>
        </div>
        <% }) %>
    </div>
    <% } else { %>
      <div class="bg-gray-50 border border-gray-100 rounded-xl p-6 text-center text-gray-500 italic">
        Tác giả này chưa có sách nào.
      </div>
      <% } %>
</div>

<%- include('../partials/pagination', { totals, page, query }) %>