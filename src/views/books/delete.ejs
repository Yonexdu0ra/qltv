<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-red-50 to-gray-50 px-4">
  <div class="max-w-md w-full bg-white rounded-2xl shadow-lg p-8 text-center border border-gray-100">
    <!-- Icon -->
    <div class="mx-auto w-14 h-14 flex items-center justify-center bg-red-100 rounded-full mb-5">
      <!-- <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path width="5px" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg> -->
      <h1 class="text-2xl font-bold text-gray-800 mb-3">Xóa sách</h1>
    </div>

    <!-- Title -->

    <!-- Error Message -->
    <%- include('../errorMessageWithQuery') %>

    <!-- Message -->
    <p class="text-gray-600 mb-6">
      Bạn có chắc chắn muốn xóa sách 
      <span class="font-semibold text-gray-900">"<%= book.title %>"</span> không?<br/>
      Hành động này <span class="text-red-600 font-medium">không thể hoàn tác</span>.
    </p>

    <!-- Buttons -->
    <form method="POST" class="flex items-center justify-between gap-4">
      <a href="/dashboard/books" 
         class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-100 transition duration-200 cursor-pointer">
        Hủy
      </a>
      <button 
        class="bg-red-600 hover:bg-red-700 text-white px-5 py-2.5 rounded-lg font-medium shadow transition duration-200 cursor-pointer">
        Xóa
      </button>
    </form>
  </div>
</div>
